/*El programa al recibir como dato un conjunto de datos de entrada
 * invierte el orden de los mismos cuando los imprime
 *A, B C y D son variables del tipo entero
 */
#include <stdio.h>
int main(int argc, char **argv )
{
	int A, B, C, D;
	printf("Ingrese cuatro valores del tipo entero : *");
	scanf("%d %d %d %d",&A,&B,&C,&D);
	printf("\n El valor de A es %d\n El valor de B es %d\n El valor de C es %d\n El valor de D es %d", A,B,C,D);
	return 0;
	}
